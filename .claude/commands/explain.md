Explain **$ARGUMENTS** in the context of Sambei's architecture.

- How it's used specifically in this codebase
- Which files implement it (search the codebase)
- Why we chose this approach (reference the relevant ADR if it exists)
- A concrete example from Sambei (not a generic example)

Keep it practical — I want to understand it well enough to implement the next feature that uses it.